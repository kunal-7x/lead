<configuration name="event_socket.conf" description="Event Socket">
  <settings>
    <!-- ESL listens on 8021; password and ACL are env-driven for local Docker and production. -->
    <param name="nat-map" value="false"/>
    <param name="listen-ip" value="0.0.0.0"/>
    <param name="listen-port" value="8021"/>
    <param name="password" value="${ESL_PASSWORD}"/>
    <param name="apply-inbound-acl" value="${ESL_APPLY_INBOUND_ACL}"/>
  </settings>
</configuration>
